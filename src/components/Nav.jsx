import classes from './Nav.module.css';

function Nav() {
  return(
    <nav>
      <a href="#">Portfolio</a>
      <a href="#">CV</a>
      <a href="#">Contact</a>
    </nav>
  );
}

export default Nav;